# GraMM-RAG / MP-DocVQA — Python dependencies
# Tested on Python 3.11, CUDA 12.4 (Lambda A100/H100)

# ── PyTorch (CUDA 12.4) ──────────────────────────────────────────────────────
# Installed via Dockerfile index URL; listed here for reference
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# ── PyTorch Geometric ────────────────────────────────────────────────────────
torch-geometric>=2.5.0
torch-scatter>=2.1.2
torch-sparse>=0.6.18

# ── NLP / Embeddings ─────────────────────────────────────────────────────────
transformers>=4.40.0
sentence-transformers>=2.7.0
accelerate>=0.29.0
bitsandbytes>=0.43.0
datasets>=2.18.0
tokenizers>=0.19.0

# ── Vision / Image ───────────────────────────────────────────────────────────
open-clip-torch>=2.24.0
Pillow>=10.0.0

# ── NLP Utilities ────────────────────────────────────────────────────────────
spacy>=3.7.0

# ── LLM APIs ─────────────────────────────────────────────────────────────────
openai>=1.23.0
together>=1.2.0

# ── Vector Search ────────────────────────────────────────────────────────────
faiss-gpu>=1.8.0

# ── Data / Evaluation ────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
evaluate>=0.4.0

# ── Config / Utils ───────────────────────────────────────────────────────────
PyYAML>=6.0.1
python-dotenv>=1.0.0

# ── Jupyter ──────────────────────────────────────────────────────────────────
jupyterlab>=4.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0
nbconvert>=7.0.0
